name: Add changeset to Renovate updates

on:
  pull_request_target:
    types: [opened, synchronize]

concurrency: ${{ github.workflow }}-${{ github.ref }}

permissions:
  contents: read # for checkout

jobs:
  call:
    uses: portabletext/.github/.github/workflows/changesets-from-conventional-commits.yml@daacf7328e445a9f87012356aa337e4d16c2dc3a # main
    if: github.event.pull_request.user.login == 'renovate[bot]'
    secrets: inherit
