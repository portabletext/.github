name: Auto format

on:
  push:
    branches: [$default-branch]

concurrency: ${{ github.workflow }}-${{ github.ref }}

permissions:
  contents: read # for checkout

jobs:
  if-needed:
    uses: portabletext/.github/.github/workflows/format.yml@74d23476005e7eae2984e2e408d06d6669d1b300 # main
    secrets: inherit

