name: Auto format

on:
  push:
    branches: [$default-branch]

concurrency: ${{ github.workflow }}-${{ github.ref }}

permissions:
  contents: read # for checkout

jobs:
  if-needed:
    uses: portabletext/.github/.github/workflows/format.yml@daacf7328e445a9f87012356aa337e4d16c2dc3a # main
    secrets: inherit

